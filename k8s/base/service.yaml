apiVersion: v1
kind: Service
metadata:
  name: vulnerable-flask-service
spec:
  type: LoadBalancer
  selector:
    app: vulnerable-flask
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000