apiVersion: v1
kind: Service
metadata:
  name: remediated-flask-service
  labels:
    app.kubernetes.io/name: remediated-flask-app
    app.kubernetes.io/part-of: devsecops-comparison-lab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: remediated-flask-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
